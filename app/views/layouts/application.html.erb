<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/bb9908bf54.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%#= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
<body>
   <header class="shadow-sm">
     <nav class="navbar navbar-expand-lg navbar-dark bg-dark " data-bs-theme="dark">
       <div class="container">
       <%= link_to 'Bookers', root_path, class: 'navbar-brand' %>

       <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
       </button>

       <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
         <% if user_signed_in? %>
          <li class="nav-item">
          <%= link_to user_path(current_user.id), class: 'nav-link text-white px-3' do %>
           <i class="fas fa-home">Home</i>
          <% end %>
          </li>
          <li class="nav-item">
                  <%= link_to users_path, class: 'nav-link text-white px-3' do %>
                    <i class="fas fa-users">Users</i>
                <% end %>
          </li>
          <li class="nav-item">
                <%= link_to books_path, class: 'nav-link text-white px-3' do %>
                    <i class="fas fa-book-open">Books</i>
                <% end %>
          </li>
          <li class="nav-item">
                <%= link_to destroy_user_session_path, method: :delete, class: 'nav-link text-white px-3' do %>
                    <i class="fas fa-sign-out-alt">Log out</i>
                <% end %>
           </li>
    <% else %>
      <li>
        <%= link_to root_path, class:'px-3 text-white' do %>
          <i class="fas fa-home">Home</i>
        <% end %>
      </li>
      <li>
         <%= link_to about_path, class:'px-3 text-white' do %>
            <i class="fas fa-link">About</i>
         <% end %>
       </li>
       <li>
          <%= link_to new_user_registration_path, class:'px-3 text-white' do %>
              <i class="fas fa-sign-in-alt">Sign up</i>
          <% end %>
        </li>
        <li>
           <%= link_to new_user_session_path, class:'px-3 text-white' do %>
               <i class="fas fa-user-plus">Log in</i>
           <% end %>
        </li>
    <% end %>
    </ul>
    </div>
    </div>
    </nav>
</header>
<main>
<% flash.each do |message_type, message| %>
      <div><%= message %></div>
<% end %>

        <%= yield %>
</main>

<footer class="mt-4">
        <p class="text-center">CopyRight Infratop.inc</p>
</footer>
</body>
</html>
