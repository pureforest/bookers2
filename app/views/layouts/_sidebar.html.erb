 　    <div class="col-md-3">
　        <h1>User info</h1>
　             <%= image_tag @user.get_profile_image(100,100) %>
　           <table> 
　            <thead class="book-head">
             　<tr>
　                <th>name</th>
                  <th><%= @user.name %><th>
               </tr>
               <tr>
　                <th>intoduction</th>
                  <th><%= @user.introduction %><th>
               </tr>
              </thead>
              </table>
              <%# if @user == current_user %>
              <%= link_to edit_user_path(@user), class:'btn btn-outline-info btn-sm mt-3 mb-5 w-50' do %>
                <i class="fas fa-user-cog"></i>
              <%# end %>
              <% end %>
              
             <h1>New book</h1>
               <% if @book.errors.any? %>
                 <%= @book.errors.count %>errors prohibited this book from being saved
                 <ul>
                 <% @book.errors.full_messages.each do |message| %>
                 <li><%= message %></li>
                 <% end %>
                </ul>
                <% end %>
                
                <%= form_with model: @book, url: books_path, method: :post do |f| %>
                <div>
                  <label>Title</label></br>
                  <%= f.text_field :title %>
                </div>
                <div>
                  <label>Opinion</label></br>
                  <%= f.text_area :body %>
                </div>
                <div>
                  <%= f.submit 'Create Book', class:'btn-succes' %>
                </div>
                <% end %>
        </div>
    